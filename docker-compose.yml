services:
  monitoring-node-exporter:
    image: prom/node-exporter:v1.11.1
    container_name: monitoring-node-exporter
    networks:
      - monitoring

  monitoring-prometheus:
    image: prom/prometheus:v3.11.3
    container_name: monitoring-prometheus
    command:
      - "--storage.tsdb.retention.time=15d"
      - "--config.file=/etc/prometheus/prometheus.yml"
    ports:
      - 9090:9090
    volumes:
      - prometheus_data:/prometheus
      - ./kustomize/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    depends_on:
      - monitoring-node-exporter
    networks:
      - monitoring

  monitoring-grafana:
    image: grafana/grafana:13.0.1
    container_name: monitoring-grafana
    ports:
      - 3000:3000
    env_file: grafana.env
    volumes:
      - grafana_data:/var/lib/grafana
      - ./kustomize/grafana/provisioning:/etc/grafana/provisioning
      - ./kustomize/grafana/dashboards:/var/lib/grafana/dashboards
    networks:
      - musiccollection
      - munchies
      - monitoring

volumes:
  prometheus_data:
  grafana_data:

networks:
  musiccollection:
    external: true
  munchies:
    external: true
  monitoring:
    name: monitoring
